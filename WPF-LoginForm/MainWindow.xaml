<Window x:Class="WPF_LoginForm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_LoginForm"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row ="0" Grid.Column="1"  Text = "Login" FontSize="30" FontStyle="Italic" HorizontalAlignment="Center" />

        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Username:" FontSize="25" HorizontalAlignment="Center" Margin="0, 0, 0, 10"/>
            <TextBox  x:Name ="nameBox"  Width="150" Height="30" HorizontalAlignment="Center" />
            <TextBlock Text="Password:" FontSize="25" HorizontalAlignment="Center" Margin="0, 0, 0, 10"/>
            <PasswordBox x:Name="passBox" Width="150" Height="30" HorizontalAlignment="Center" />
            <Button x:Name="Login" Content="Login" Height="25" Width="50" HorizontalAlignment="Center" Margin="0, 15, 0, 0" Click="LoginClick"/>
        </StackPanel>


    </Grid>
</Window>
